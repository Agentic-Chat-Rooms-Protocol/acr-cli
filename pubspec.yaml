name: acr_cli
description: Agentic Chat Rooms (ACR) Native CLI — High-performance terminal toolkit for multi-agent deliberation, Byzantine Quorum consensus, Atlas 2.0 DAG, and autonomous OpsRoom war room.
version: 0.9.0
repository: https://github.com/Agentic-Chat-Rooms-Protocol/acr-cli

environment:
  sdk: ^3.13.1

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  ffi: ^2.2.0
  http: ^1.6.0
  io: ^1.0.5
  stack_trace: ^1.12.1

dev_dependencies:
  ffigen: ^21.0.0
  lints: ^6.0.0
  test: ^1.25.6
